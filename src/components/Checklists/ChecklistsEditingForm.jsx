import { View, TouchableOpacity, TextInput } from "react-native";
import FontAwesome from "@fortawesome/react-native-fontawesome";

export default function ChecklistsEditingForm(props) {
  
  
  return <View></View>;
}
